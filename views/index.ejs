<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      rel="stylesheet"
      href="https:/cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css"
    />
    <link rel="stylesheet" href="/css/style.css" />
    <title>Youtube MP3 Converter</title>
  </head>
  <body>
    <div class="top-container">
      <form action="/search-video" method="POST" id="form">
        <h1><i class="fab fa-youtube"></i>Song 2 U</h1>
        <h4>Insira abaixo o link do YouTube</h4>
        <div>
          <input type="text" name="videoUrl" placeholder="Link do YouTube" />
          <button id="convert-btn">Buscar</button>
          <div id="loading" style="display: none">🔄 Carregando...</div>
        </div>
      </form>
    </div>

    <div class="container">
      <% if (typeof valid != "undefined" && valid ) { %>
      <h2><%= message %></h2>
      <h3>Título: <%= videoData.video_details.title %></h3>
      <img src="<%= videoData.video_details.thumbnail.url %>" alt="Thumbnail" />
      <% } else { %>
      <h2><%= message %></h2>
      <% } %>
    </div>

    <div class="bottom-container">
      <% if(typeof success != "undefined" && success) { %>
      <div class="success">
        <p><%= song_title %></p>
        <a href="<%= song_link %>"
          ><button id="download-btn">DOWNLOAD</button></a
        >
      </div>
      <% } else if(typeof success != "undefined" && !success) {%>
      <div class="errors">
        <p><%= message %></p>
      </div>
      <% } %>
    </div>
  </body>
</html>
